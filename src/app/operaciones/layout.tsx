/**
 * Layout del grupo "Operaciones" (Control de Negocio, Logística, Causa Raíz).
 *
 * Este sprint: passthrough mínimo — sólo renderiza children.
 * Futuro: puede agregar tabs internas / breadcrumb compartido cuando los
 * tres sub-módulos estén vivos.
 */

export default function OperacionesLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
